真正的**邻接表（adjacency list）**其实是你代码里的 fanout_ 和 fanin_

fanout_["U1/A"] = {{"U1/Y", 0.3f}, ...} → 从 U1/A 出发的边
fanin_["U2/A"] = {{"U1/Y", 0.0f}, ...} → 到达 U2/A 的边
你用 pin_map_ 来完成步骤 1（名字解析），然后在 finalize() 里拿解析好的指针去填 fanout_ 和 fanin_（步骤 2），邻接表就建好了。

6.21
下一个问题：为什么要用 optional<float> arrival_time;  // Phase 2: timing state
    optional<float> req_time;

6.30
 Step 1：补全头文件 timing_engine.h 的类声明

  你现在的情况是 class TimingEngine 体是空的，下面一大块被注释掉的接口定义。把这部分取消注释、整理成真正的类声明：

  class TimingEngine {
  public:
      // 前向传播：计算所有 pin 的 arrival_time
      void propagate_arrival_time(TimingGraph& tg);

      // 反向传播：计算所有 pin 的 req_time
      void propagate_required_time(TimingGraph& tg, float clock_period);

      // 计算单个 pin 的 slack = RAT - AT
      static float slack(const Pin& pin);
  };

  提示：
  - slack 是 static 的，因为它不依赖 TimingEngine 对象的任何状态，只操作传入的 Pin
  - propagate_* 接收 TimingGraph&，通过其 fanout()/fanin()/lookup_pin() 接口操作

  ---
  Step 2：实现 timing_engine.cpp

  2a. propagate_arrival_time — 关键代码骨架

  void TimingEngine::propagate_arrival_time(TimingGraph& tg) {
      // 1. 重置所有 arrival_time
      for (const auto& cell : tg.cells())
          for (const auto& pin : cell.Pins)
              pin.arrival_time.reset();

      // 2. 找到 startpoints 并 seed
      queue<string_view> worklist;
      for (const auto& cell : tg.cells())
          for (const auto& pin : cell.Pins) {
              string qn = cell.name + "/" + pin.name;
              if (tg.fanin(qn).empty()) {          // ← 没有扇入 => startpoint
                  pin.arrival_time = 0.0f;          // ← seed
                  worklist.push(qn);
              }
          }

      // 3. Worklist 主循环
      while (!worklist.empty()) {
          // pop...
          // 遍历 tg.fanout(cur)
          //   new_at = cur_at + edge.delay
          //   if (new_at > dst_arrival) → 更新并推入 worklist
      }
  }

7.4
bellman ford算法
SPFA
Dijkstra

7.13
问题 1：这不是 TimingEngine 的成员函数 ❌
     
  你写的是命名空间 tsta 下的自由函数：
     
  vector<string> trace_critical_path(const TimingGraph& tg){  // 自由函数

  但头文件里注释的声明是 TimingEngine 的成员函数：

  std::vector<std::string> trace_critical_path(
      const TimingGraph& tg, const std::string& endpoint) const;  // 成员函数

  至少加 TimingEngine:: 前缀：
  vector<string> TimingEngine::trace_critical_path(const TimingGraph& tg){

  ---
  问题 2：auto-detect endpoint vs 作为参数传入 🤔

  你写的：「访问fanin_，寻找没有fanout的pin作为终点」

  这里有两个子问题：

  a) 你访问不了 fanin_ — fanin_ 是 TimingGraph 的私有成员。要用 tg.fanin(name) 和 tg.fanout(name) 公共接口。

  b) "自动找终点" vs "传参指定终点"

  ┌─────────────────────────────┬──────────────────────────────────────────────┬─────────────────────────────────────────────────┐
  │            方案             │                     优点                     │                      缺点                       │
  ├─────────────────────────────┼──────────────────────────────────────────────┼─────────────────────────────────────────────────┤
  │ 你的：(tg) 自动找           │ 调用方便，不用自己传                         │ 假如多个 endpoint 都有负 slack，该 trace 哪个？ │
  ├─────────────────────────────┼──────────────────────────────────────────────┼─────────────────────────────────────────────────┤
  │ 原设计：(tg, endpoint) 指定 │ 精确控制从哪开始 trace，可逐个 endpoint 调用 │ 调用前得先知道有哪些 endpoint                   │
  └─────────────────────────────┴──────────────────────────────────────────────┴─────────────────────────────────────────────────┘

  推荐原设计 (tg, endpoint)，原因是：
  - 在 real STA 中，你会对所有 endpoint 都 trace 一遍（report_timing）
  - print_report() 里会这么用：先找到 slack 最差的 endpoint，然后 trace_critical_path(tg, worst_endpoint)
  - 职责单一：trace 就只是 trace，不负责找 endpoint

  如果你想要一个"自动选最差 endpoint 然后 trace"的便利包装，可以在 print_report() 里做，trace_critical_path 本身保持参数化：

  // 核心：从指定 endpoint 回溯
  vector<string> trace_critical_path(const TimingGraph& tg, const string& endpoint);

  // 包装：自动选 slack 最差的 endpoint（在 print_report 里实现）

  ---
  问题 3：代码还缺 reverse 和需要的头文件

  回溯时是从 endpoint → startpoint（反向），最后要 reverse：
  #include <algorithm>  // for std::reverse

  ---
  英语纠正

  ▎ 访问fanin_,寻找没有fanout的pin作为终点

  → "Find endpoints (pins with no fanout) by checking tg.fanout(pin_name)"

  ---
  总结：建议这么写声明

  先恢复头文件的声明（取消注释 timing_engine.h:60-61）：

  std::vector<std::string> trace_critical_path(
      const TimingGraph& tg, const std::string& endpoint) const;

  然后在 timing_engine.cpp 里：

  vector<string> TimingEngine::trace_critical_path(
      const TimingGraph& tg, const string& endpoint) const
  {
      // 实现
  }

  要不要我帮你把头文件恢复声明，然后写出 trace_critical_path 的完整实现？